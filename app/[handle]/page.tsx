// /workspaces/Vext/app/[handle]/page.tsx
// Root handles like /trimac should resolve as BUSINESS handles.
// We just reuse the existing /c/[handle] page.

export { default } from '../c/[handle]/page';
